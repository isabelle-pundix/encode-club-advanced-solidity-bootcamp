Optimising storage

See contract: https://gist.github.com/extropyCoder/6e9b5d5497b8ead54590e72382cdca24

Slots using initially:

1. Slot 0:

- `uint8 index`: 1 byte

2. Slot 1:

- `uint256 number`: 32 bytes

3. Slot 2:

- `address admin`: 20 bytes
- `bool flag1`: 1 byte

4. Slot 3:

- `mapping(address => uint256) balances`: storage slot for reference

5. Slot 4:

- `address admin2`: 20 bytes
- `bool flag2` and `bool flag3`: 1 byte each

`topPayments[8]` array
Struct breakdown for each (`payments`):

1. Slot 1:

- `bool valid`: 1 byte

2. Slot 2:

- `uint256 amount`: 32 bytes

3. Slot 3:

- `uint8 paymentType`: 1 byte
- `address sender`: 20 bytes

4. Slot 4:

- `uint256 finalAmount`: 32 bytes

5. Slot 5:

- `address receiver`: 20 bytes

6. Slot 6:

- `uint256 initialAmount`: 32 bytes

7. Slot 7:

- `bool checked`: 1 byte

Total slots for `topPayments[8]`: 56 slots

### **Total Slots used initially**: 5 + 56 = 61

After rearranging:

1. Slot 0:

- `uint8 index`: 1 byte
- `address admin`: 20 bytes
- `bool flag1`: 1 byte
- `bool flag2` and `bool flag3`: 1 byte each

2. Slot 1:

- `address admin2`: 20 bytes

3. Slot 2:

- `uint256 number`: 32 bytes

4. Slot 3:

- `mapping(address => uint256) balances`: storage slot for reference

`topPayments[8]` array
Struct breakdown for each (`payments`):

1. Slot 1:

- `address sender`: 20 bytes
- `uint8 paymentType`: 1 byte
- `bool checked`: 1 byte
- `bool valid`: 1 byte

2. Slot 2:

- `address receiver`: 20 bytes

3. Slot 3:

- `uint256 amount`: 32 bytes

4. Slot 4:

- `uint256 finalAmount`: 32 bytes

5. Slot 5:

- `uint256 initialAmount`: 32 bytes

Total slots for `topPayments[8]`: 40 slots
